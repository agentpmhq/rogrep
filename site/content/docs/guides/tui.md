---
layout: docs.njk
title: Terminal UI
description: Browse rogrep interactively in a terminal.
---
Run `rogrep tui`, optionally with a query, conversation ID, or exchange reference. An ID opens the conversation directly; other text pre-fills search. The TUI requires a real interactive terminalautomation and headless agents should use text or JSON commands.
