---
layout: docs.njk
title: Statistics
description: Run deterministic local usage reports.
---
`rogrep stats` reports from SQLite without an LLM. Views include daily, weekly, monthly, heatmap, tools, Git activity, top exchanges, and projects. Global options include `--since`, `--timezone`, and `--json`. See every view in the [CLI index](/docs/cli/stats/).
