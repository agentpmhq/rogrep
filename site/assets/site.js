document.querySelectorAll("pre").forEach(p=>p.tabIndex=0);const menu=document.querySelector(".menu"),nav=document.querySelector("#nav");menu?.addEventListener("click",()=>{const open=menu.getAttribute("aria-expanded")!=="true";menu.setAttribute("aria-expanded",String(open));nav.classList.toggle("open",open)});document.querySelectorAll(".copy").forEach(b=>b.addEventListener("click",async()=>{const text=b.parentElement.querySelector("code")?.innerText||"";b.textContent="Copied";await navigator.clipboard.writeText(text).catch(()=>{});setTimeout(()=>b.textContent="Copy",1600)}));const dialog=document.querySelector("#search");async function openSearch(){dialog.showModal();const box=document.querySelector("#search-box");if(box.dataset.ready)return;box.dataset.ready="1";await import("/pagefind/pagefind-ui.js");new PagefindUI({element:"#search-box",showSubResults:true})}document.querySelector(".search-open")?.addEventListener("click",openSearch);addEventListener("keydown",e=>{if(e.key==="/"&&!/input|textarea/i.test(document.activeElement.tagName)){e.preventDefault();openSearch()}});
